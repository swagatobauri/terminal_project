/**
 * Stub commands — to be built out in future iterations.
 * Each file exports a run function that the CLI wires up.
 */

export async function runLog(_options) {
  console.log('chronicl log — coming soon.');
}
